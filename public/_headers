# Content-hashed build output: safe to cache forever.
# Previously injected by @astrojs/cloudflare; kept here now that the adapter is gone.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
